fetch('/api/session').then(response=>response.ok?response.json():null).then(session=>{const user=session?.user;if(!user)return;const show=user.role!=='sale'||Boolean(user.team),column=9,header=document.querySelectorAll('thead th')[column];if(show){header.textContent='SALE';return}header.hidden=true;document.querySelectorAll('#rows tr').forEach(row=>row.cells[column].hidden=true)});
